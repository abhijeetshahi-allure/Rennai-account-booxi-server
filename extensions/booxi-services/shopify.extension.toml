# Learn more about configuring your Customer account UI extension:
# https://shopify.dev/api/customer-account-ui-extensions/unstable/configuration

# The version of APIs your extension will receive. Learn more:
# https://shopify.dev/docs/api/usage/versioning
api_version = "2026-01"

[[extensions]]
name = "booxi-services"
handle = "booxi-services"
type = "ui_extension"
uid = "b7dd29b3-704a-29d9-0e7d-2d0db1448d2a2f5e55b1"

# Controls where in Shopify your extension will be injected,
# and the file that contains your extension’s source code. Learn more:
# https://shopify.dev/docs/api/customer-account-ui-extensions/unstable/extension-targets-overview

[[extensions.targeting]]
module = "./src/Extension.jsx"
target = "customer-account.page.render"

[extensions.capabilities]
# Gives your extension access to directly query Shopify’s storefront API.
# https://shopify.dev/docs/api/customer-account-ui-extensions/unstable/configuration#api-access
api_access = true

# Gives your extension access to make external network calls, using the
# JavaScript `fetch()` API. Learn more:
# https://shopify.dev/docs/api/customer-account-ui-extensions/unstable/configuration#network-access
network_access = true

[extensions.settings]
[[extensions.settings.fields]]
key = "backend_url"
type = "single_line_text_field"
name = "Booxi API backend URL"
description = "Your app backend URL (e.g. https://xxx.trycloudflare.com for dev). Get from terminal when running shopify app dev."

[[extensions.settings.fields]]
key = "brand_name"
type = "single_line_text_field"
name = "Brand name"
description = "Brand name shown on booking cards (e.g. Rennai). Leave empty to hide."
